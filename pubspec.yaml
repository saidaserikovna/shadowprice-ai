name: shadowprice_ai
description: ShadowPrice AI — Детектор скрытых переплат
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^3.8.1
  firebase_auth: ^5.3.4
  cloud_firestore: ^5.6.0
  google_sign_in: ^6.2.2
  fl_chart: ^0.70.2
  provider: ^6.1.2
  cached_network_image: ^3.4.1
  http: ^1.2.2
  html: ^0.15.5
  intl: ^0.19.0
  shared_preferences: ^2.3.4
  url_launcher: ^6.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/logo.png
